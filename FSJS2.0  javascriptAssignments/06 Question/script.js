//'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon'
// split the string at the comma and change it to an array.

var variable = "Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon"

var myArray = variable.split(" ")
console.log(myArray);